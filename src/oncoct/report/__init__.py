"""Report schema and generation.

The schema (schema.py) is the machine-readable contract the agent must satisfy; the
generator (generator.py) turns per-lesion tool outputs into a structured report plus
a trace linking every number to the tool call that produced it.
"""
