"""Deterministic lesion measurement (RECIST 1.1, volume, centroid).

Measurements are rule-based and reproducible — never estimated by the LLM. The agent
cites these numbers; it does not produce them. See tests/test_recist.py.
"""
